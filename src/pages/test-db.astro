---
// calling this route should test database connection to the front-end without involving the RAG pipeline
const res = await fetch("/api/test-db");
const data = await res.json();
---

<h1>Database Test</h1>
<p><strong>Your PostgreSQL NOW():</strong> {data.now}</p>